{
  "name": "app-capture",
  "version": "0.1.0",
  "private": true,
  "description": "Local Android APK screen capture tool for Figma import.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/android.js && node --check src/exporter.js && node --check src/layout.js && node --check src/preload.js && node --check src/renderer/renderer.js && node --check figma-plugin/code.js"
  },
  "devDependencies": {
    "electron": "^42.5.0"
  }
}
